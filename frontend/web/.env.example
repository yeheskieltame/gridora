NEXT_PUBLIC_TESTNET=true
NEXT_PUBLIC_BSC_RPC_URL=https://bsc-testnet.publicnode.com
NEXT_PUBLIC_IDENTITY_ADDR=
NEXT_PUBLIC_JOURNAL_ADDR=
NEXT_PUBLIC_LEDGER_ADDR=
# Operator console: where the backend control API lives (runner --serve). The console
# at /console reads state from here and unlocks controls after an owner-wallet signature.
NEXT_PUBLIC_CONTROL_API=http://127.0.0.1:8317
